const readline = require("readline-sync");

let base = Number(readline.question("Digite o valor da base: "));
let expoente = Number(readline.question("Digite o valor do exponte: "));
let valor = 1;

while (expoente > 0) {
    valor *
    = base;
    expoente--;
}
console.log(valor);
